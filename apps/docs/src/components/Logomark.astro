---
// apps/docs/src/components/Logomark.astro
import LogoPaths from './LogoPaths.astro'

interface Props {
  class?: string
}

const { class: className, ...props } = Astro.props
---

<svg aria-hidden="true" viewBox="0 0 36 36" fill="none" class={className} {...props}>
  <LogoPaths />
</svg>
